# LINE 三產品架構與共用技術規劃

> 專案：`hub-google/LINE`
> 目標：維持三個獨立產品、獨立客群與獨立商業模式，同時在不影響產品邊界的前提下，逐步抽取可安全共用的 LINE 技術元件。

---

## 1. 核心定位

此 repository 不是「一個產品的三個功能模組」，而是：

> **三個彼此獨立的 LINE 相關產品，共存於同一個 repository。**

目前三個產品：

```text
LINE/
├── store-operations/
├── group-cleanup/
└── recurring-billing/
```

三個產品可以：

- 面向不同客群。
- 使用不同商業模式。
- 使用不同部署方式。
- 使用不同資料庫。
- 使用不同 LINE OA。
- 使用不同登入流程。
- 使用不同品牌名稱與網域。
- 獨立開發、測試、部署與銷售。

共同點只有：

> 都與 LINE 生態系有關。

因此不得因為放在同一個 repository，就強迫三個產品共享同一個 Portal、同一個 Merchant、同一套 User、同一個 LINE OA 或同一套資料庫。

---

## 2. 三個產品定位

### 2.1 store-operations

產品類型：

> LINE OA 門市營運系統

主要客群：

- 餐飲店
- 美髮／美容
- 診所
- 服務業
- 需要預約、取號、叫號、點餐、付款的店家

主要使用者：

- 消費者
- 店員
- 店長
- 商家管理員

核心技術：

- LINE Official Account
- LIFF / LINE MINI App
- Messaging API
- Rich Menu
- LINE Login
- LINE Pay
- PostgreSQL / Prisma
- Web App

既有功能包括：

- 預約
- 現場取號
- 即時叫號
- 過號
- 回叫
- 插隊
- 開始服務
- 完成服務
- 商品／餐點／服務品項
- 訂單
- 現場追加
- LINE Pay
- Manual Payment
- 部分付款
- item-level payment
- Staff / Manager / Owner
- Per-user Rich Menu
- 多商家
- 多分店
- Audit Log
- REST API
- Mock LINE Services
- 自動化測試

主要既有頁面：

```text
/c
/c/queue
/c/reserve
/c/order
/c/my
/c/pay

/s
/s/orders
/s/reservations
/s/staff
/s/invite
```

---

### 2.2 group-cleanup

產品類型：

> 一般 LINE 使用者的桌面群組整理與自動化工具

主要客群：

- LINE 群組很多的一般使用者
- 社群管理者
- 活動主辦人
- 專案型群組重度使用者
- 想批次清理舊群組的人

主要使用者：

- 一般 LINE 個人帳號使用者

核心技術：

- LINE Windows Desktop
- Python
- PyAutoGUI
- Windows UI Automation
- 本機 GUI
- EXE
- 可選 Cloud Sync
- 可選 LINE Bot / Messaging API 輔助流程

重要產品邊界：

> LINE Messaging API 無法代替個人 LINE 帳號執行退群、踢人或操作聊天室。

因此此產品的核心操作必須保留在：

```text
Windows
  ↓
LINE Desktop
  ↓
Desktop Automation
```

LINE OA / Bot 僅能作為部分功能的輔助工具，例如：

- 解散公告
- 72 小時公示
- 成員異議
- Flex Message
- Webhook
- Cloud Scheduler

既有功能包括：

- LINE Windows 電腦版連接與定位
- 群組掃描
- 群組搜尋
- 群組選擇
- 白名單
- 批次退群
- 批次解散
- 自動邀請 Bot
- PyAutoGUI
- Windows UI Automation
- 圖像比對備援
- ui_mapping
- 執行前二次確認
- Rate Limiting
- 模擬真人操作延遲
- 緊急中止
- 錯誤跳過
- Audit Log
- JSON DB
- Google Sheets DB
- Cloud Sync
- FastAPI Server
- Scheduler
- Flex Message
- HMAC
- 即時退群模式
- 72 小時公示模式
- 成員異議取消
- Bot 被踢出時終止任務
- READY_FOR_DISSOLUTION
- GUI Dashboard
- Group List
- Settings
- Confirm Dialog
- EXE 打包
- 自動化測試

---

### 2.3 recurring-billing

產品類型：

> LINE 定期收款與對帳提醒工具

主要客群：

- 小型團體
- 社團
- 房東
- 家庭
- 教練／老師
- 小型商家
- 固定週期收款者

主要使用者：

- Initiator
- Payer

核心技術：

- LINE Official Account
- LIFF
- LINE Login
- Messaging API
- Flex Message
- 排程
- JWT
- PostgreSQL
- Web App

目前產品原則：

> 系統主要負責「提醒、帳本、對帳狀態」，不一定直接介入實際金流。

既有功能包括：

- 建立收款專案
- 固定收款金額
- 每月繳費日
- LINE UID
- LIFF
- JWT
- INITIATOR / PAYER
- 邀請成員加入
- Transaction
- Billing Month
- UNPAID
- PENDING
- COMPLETED
- OVERDUE
- notifiedAt
- paidAt
- confirmedAt
- Cron
- 繳費日前提醒
- LINE Push
- 付款入口
- 人工付款回報
- 發起人確認
- 歷史付款紀錄

目前既有 API：

```text
/api/auth
/api/projects
/api/projects/[id]/join
/api/transactions/[id]/pay
/api/transactions/[id]/confirm
/api/cron/remind
/api/webhook
```

---

## 3. 商業面原則

三個產品不可因為技術共用而被迫包成單一產品。

建議維持：

```text
Product A
store-operations
→ 店家 SaaS

Product B
group-cleanup
→ 個人工具 / Desktop App

Product C
recurring-billing
→ 個人 / 小團體收款工具
```

三者可各自擁有：

- 產品名稱
- Landing Page
- 定價
- 訂閱方案
- 免費方案
- 登入頁
- LINE OA
- 客服管道
- 使用條款
- 隱私政策
- 資料庫
- 部署環境

不建立「使用者買一套就必須看到三個功能」的假設。

---

## 4. Repository 原則

目前建議 repository 結構維持：

```text
LINE/
│
├── store-operations/
│
├── group-cleanup/
│
├── recurring-billing/
│
├── packages/
│   └── （未來真正有需要時才建立共用套件）
│
├── docs/
│
├── README.md
└── AGENTS.md
```

目前不要求立即改成 monorepo framework。

只有在重複程式碼確實造成維護成本時，才建立：

```text
packages/
├── line-messaging/
├── line-auth/
├── liff-utils/
├── webhook-utils/
├── shared-types/
└── shared-ui/
```

共用套件必須以「有兩個以上產品真的需要」為前提。

不得為了架構漂亮而抽象。

---

## 5. 可以共用的技術

以下能力具備潛在共用價值：

### 5.1 LINE Messaging API Client

可共用：

- Push Message
- Reply Message
- Flex Message
- Error Handling
- Retry
- API wrapper

適用：

- store-operations
- recurring-billing
- group-cleanup 的 Bot 輔助功能

---

### 5.2 Webhook 驗證

可共用：

- LINE Signature 驗證
- Webhook Event Parser
- 基礎型別
- 共用測試資料

但三個產品的 webhook endpoint 不必統一。

例如：

```text
store.example.com/api/webhook/line
billing.example.com/api/webhook/line
groups.example.com/api/webhook/line
```

---

### 5.3 LIFF 工具

可共用：

- LIFF 初始化
- Token 取得
- Login helper
- Profile helper
- token → backend 驗證流程範本

主要適用：

- store-operations
- recurring-billing
- group-cleanup 的公示／異議頁面

---

### 5.4 LINE Login / Token 驗證

可共用驗證邏輯或 library。

但不要求共享 User Table。

例如：

```text
store user
billing user
group user
```

可以完全是不同資料庫裡的不同 record。

---

### 5.5 Flex Message Builder

可以建立共用：

```text
packages/flex-message/
```

提供：

- Button
- Card
- Header
- Receipt
- Reminder
- Invitation

但各產品仍保留自己的 message template。

---

### 5.6 共用 CI / Testing 工具

可以共用：

- lint config
- TypeScript config
- GitHub Actions templates
- Playwright setup
- test fixtures
- security checks
- dependency update workflow

這類共用風險最低，也最值得優先整理。

---

## 6. 不應強制共用的東西

以下項目不應因為三個產品都跟 LINE 有關，就強制整合。

### 6.1 User

不要求三個產品共用 User Table。

原因：

- 客群不同
- 使用情境不同
- 登入方式不同
- group-cleanup 甚至可能完全不需要雲端帳號

---

### 6.2 Merchant

Merchant 只適合 store-operations。

recurring-billing 的主體可能是：

- 個人
- 家庭
- 社團
- 小型組織

group-cleanup 更沒有 Merchant 概念。

因此不得將 Merchant 當成 repository 頂層資料模型。

---

### 6.3 Portal

不建立：

```text
一個 Dashboard
├── 門市
├── 收款
└── 退群
```

除非未來真的有大量使用者需要同時使用三者。

目前沒有這個產品假設。

---

### 6.4 Database

三個產品可以維持各自資料庫。

例如：

```text
store-operations
→ PostgreSQL

recurring-billing
→ PostgreSQL

group-cleanup
→ Local JSON / SQLite / Cloud DB
```

只有未來出現明確跨產品資料需求，才考慮整合。

---

### 6.5 Payment

store-operations 的 Payment 是：

- 訂單付款
- LINE Pay
- 現場付款
- 部分付款
- Item-level Payment

recurring-billing 的核心是：

- 收款提醒
- 付款回報
- 人工確認
- 帳款狀態

兩者語意不同。

可以共用底層 utility，但不應強迫共用同一 Payment Domain。

---

### 6.6 Scheduler

排程技術可以共用 library 或部署方法。

但 job 不需要跑在同一個 scheduler instance。

例如：

```text
store
→ reservation reminder

billing
→ monthly payment reminder

groups
→ dissolution countdown
```

應保持產品隔離。

---

## 7. group-cleanup 特殊架構

group-cleanup 與另外兩個產品不同，必須視為 Desktop-first 產品。

推薦架構：

```text
┌──────────────────────────┐
│      Desktop App         │
│                          │
│ Python GUI               │
│ Automation Engine        │
│ Whitelist                │
│ Local Settings           │
│ Emergency Stop           │
└────────────┬─────────────┘
             │
             ▼
       LINE Desktop
```

若啟用 Cloud 功能：

```text
Desktop App
     │
     │ HTTPS
     ▼
Optional Cloud Service
     │
     ├── Task State
     ├── Scheduler
     ├── Bot Webhook
     ├── 72h Notice
     └── Cancellation
```

Cloud 不是產品正常啟動的必要前提，除非該功能確實需要。

---

## 8. 三產品之間的依賴原則

產品之間不得直接互相 import 對方 Domain。

禁止：

```text
store-operations
  ↓
import recurring-billing/src/payment

group-cleanup
  ↓
import store-operations/src/user
```

正確做法：

若能力真的共用，先抽到：

```text
packages/
```

再由各產品依賴：

```text
store-operations ─┐
                  ├─ packages/line-messaging
recurring-billing ┘
```

這樣才能保持產品可以獨立部署。

---

## 9. 建議的產品部署方式

### store-operations

```text
Web App
Backend API
PostgreSQL
LINE OA
LINE Pay
```

部署可以是：

- Vercel
- Cloud Run
- Railway
- Render
- Supabase / Neon / Cloud SQL

---

### recurring-billing

```text
Web App
Backend API
PostgreSQL
Scheduler
LINE OA
```

可獨立於 store-operations 部署。

---

### group-cleanup

```text
Windows EXE
+
Optional Cloud Backend
+
Optional LINE OA Bot
```

主要交付物是 Desktop Application。

---

## 10. 未來共用套件策略

不要一次建立大量 Shared Core。

採用「重複出現才抽」原則：

### 第一次出現

留在產品內。

### 第二個產品需要

先比較需求是否真的一致。

### 需求高度一致

再抽到 `packages/`。

### 需求只是表面相似

維持分開。

範例：

```text
LINE signature verification
→ 幾乎完全一致
→ 適合共用

Payment
→ 商家訂單付款 vs 定期收款對帳
→ Domain 不一致
→ 不應共用
```

---

## 11. 開發與 Agent 原則

後續 Codex / Antigravity / Agent 在處理此 repository 時必須遵守：

### 原則 1：先辨識產品

修改前先確認任務屬於：

- store-operations
- group-cleanup
- recurring-billing
- shared package

不可因為都在 LINE repo 就跨產品修改。

### 原則 2：產品隔離優先

預設：

> 一個產品的修改，不應影響另外兩個產品。

### 原則 3：不要自行合併 Domain

Agent 不得自行將：

- User
- Merchant
- Payment
- Database
- Auth
- API
- Portal

合成一套。

除非任務明確要求。

### 原則 4：共用程式必須有實際需求

只有至少兩個產品真的存在同一問題，才考慮抽 package。

### 原則 5：保留既有功能

重構不得造成功能消失。

---

## 12. 建議 migration 計畫

### Phase 0：完成產品邊界整理

已完成資料夾命名：

```text
LINE群組管理 → store-operations
LINE退群     → group-cleanup
定期支付     → recurring-billing
```

---

### Phase 1：更新文件與路徑

需要逐步檢查：

- README
- docs
- scripts
- GitHub Actions
- deploy scripts
- file path reference
- AGENTS.md
- local development instructions

把舊資料夾名稱更新為新名稱。

---

### Phase 2：確保三個產品可獨立啟動

每個產品都應有自己的：

- README
- 環境變數說明
- 啟動方式
- 測試指令
- 部署方式
- 功能清單

目標：

```text
cd store-operations
→ 可以獨立開發

cd recurring-billing
→ 可以獨立開發

cd group-cleanup
→ 可以獨立開發
```

---

### Phase 3：建立共用候選清單

只盤點，不急著搬。

例如：

- LINE signature verifier
- Messaging API wrapper
- LIFF helpers
- Flex Message helpers
- lint config
- test utilities

---

### Phase 4：先抽低風險共用能力

優先：

1. config
2. typings
3. LINE webhook signature
4. LINE API basic client
5. Flex utilities
6. CI utilities

暫時不要抽：

- User
- Merchant
- Payment
- Database
- Business Domain

---

### Phase 5：商業化獨立

三個產品分別建立：

- Landing Page
- 定價
- Demo
- onboarding
- 使用條款
- 隱私政策
- 銷售策略
- 客群驗證

三個產品可以完全不同。

---

## 13. 成功條件

本 repository 整理完成後應符合：

1. 三個產品名稱與實際功能一致。
2. 三個產品可以獨立理解。
3. 三個產品可以獨立開發。
4. 三個產品可以獨立測試。
5. 三個產品可以獨立部署。
6. 三個產品不需要共享同一登入。
7. 三個產品不需要共享同一資料庫。
8. 三個產品不需要共享同一 LINE OA。
9. group-cleanup 保留 Desktop-first 架構。
10. store-operations 保留完整門市營運功能。
11. recurring-billing 保留完整週期性收款功能。
12. 真正重複的基礎技術可以逐步抽成 package。
13. 不為了「統一架構」破壞產品獨立性。
14. 不因重構而遺失既有功能。

---

## 14. 最終方向

此 repository 的正確定位應為：

```text
hub-google/LINE

├── store-operations
│   └── LINE OA 店家營運產品
│
├── recurring-billing
│   └── LINE 定期收款產品
│
├── group-cleanup
│   └── LINE 個人群組整理產品
│
└── packages
    └── 真正需要時才抽出的共用 LINE 技術元件
```

最終原則：

> **Three products, one repository, shared code only where it genuinely makes sense.**

也就是：

> **產品不合併，工程資產可以共用。**
