# LINE 三大功能合併架構與遷移計畫

> 專案：`hub-google/LINE`
> 目標：將目前三個既有子專案整合為單一 LINE 平台，原有功能全部保留，不因合併而刪減既有能力。

## 1. 合併目標

目前 repository 由三個相對獨立的子專案組成：

```text
LINE/
├── LINE群組管理/
├── LINE退群/
└── 定期支付/
```

合併後改為「一個主系統 + 三個子功能模組」：

```text
LINE Platform
├── Store Ops（門市營運）
├── Group Ops（群組管理與自動化）
└── Billing（定期收款）
```

本次整併原則：**現有功能全部保留，先整合架構，再逐步消除重複實作。**

---

## 2. 原有功能盤點

### 2.1 LINE群組管理

目前實際上已是完整的 LINE OA 門市營運系統，而非單純群組管理。

既有功能包括：

- LINE Login / LIFF 身分驗證
- 顧客端與商家端權限劃分
- OWNER / MANAGER / STAFF RBAC
- Merchant / Branch 多商家、多分店架構
- Per-user Rich Menu
- 現場排隊取號
- 即時叫號
- 過號、回叫、插隊、開始服務、完成服務
- 預約時段管理
- 預約容量控制
- 預約報到
- 預約轉號碼牌
- 商品、服務、餐點、加購品管理
- Order / Order Item
- 現場追加服務
- LINE Pay Request / Confirm
- Manual Payment
- 部分付款、完整付款
- Payment State Machine
- Idempotency
- Audit Log
- LINE Messaging API
- LINE Webhook
- Customer Portal
- Staff / Admin Portal
- PostgreSQL
- Prisma ORM
- Domain Layer
- REST API
- Mock LINE Services
- 自動化測試與架構文件

主要既有頁面：

```text
/c
/c/queue
/c/reserve
/c/order
/c/my
/c/pay

/s
/s/orders
/s/reservations
/s/staff
/s/invite
```

---

### 2.2 LINE退群

此專案實際上是一套 LINE Desktop Group Automation System。

既有功能包括：

- LINE Windows 電腦版連接與定位
- 群組掃描
- 群組搜尋
- 群組選擇
- 批次處理
- 自動退群
- 自動邀請 Bot
- PyAutoGUI
- Windows UI Automation
- 圖像比對備援
- ui_mapping 設定
- 白名單
- 防止誤退群
- 執行前確認
- Rate Limiting
- 模擬真人操作延遲
- 緊急中止
- 錯誤跳過
- Audit Log
- JSON DB
- Google Sheets DB
- Cloud Sync
- LINE Bot
- LINE Webhook
- FastAPI Server
- Scheduler
- Flex Message
- 安全 Token / HMAC
- 即時退群模式
- 72 小時公示模式
- 成員異議取消
- Bot 被踢出時終止任務
- READY_FOR_DISSOLUTION 狀態
- GUI Dashboard
- Group List
- Settings
- Confirm Dialog
- EXE 打包
- 自動化測試

---

### 2.3 定期支付

目前為 LINE 週期性收款與繳費管理系統。

既有功能包括：

- LINE Login
- LINE UID
- JWT
- LIFF
- LINE Bot SDK
- Project
- Initiator
- 固定收款金額
- 每月繳費日
- Membership
- INITIATOR / PAYER
- 使用者加入專案
- Transaction
- Billing Month
- UNPAID
- PENDING
- COMPLETED
- OVERDUE
- notifiedAt
- paidAt
- confirmedAt
- Cron
- 繳費日前提醒
- LINE Push
- LIFF 付款入口
- 付款狀態確認
- 歷史付款紀錄

目前既有 API：

```text
/api/auth
/api/projects
/api/projects/[id]/join
/api/transactions/[id]/pay
/api/transactions/[id]/confirm
/api/cron/remind
/api/webhook
```

---

## 3. 合併後產品定位

合併後 repository 應視為單一產品：

# LINE Business OS / LINE 營運平台

架構：

```text
                    LINE Platform
                         │
              ┌──────────┴──────────┐
              │     Shared Core      │
              └──────────┬──────────┘
                         │
       ┌─────────────────┼─────────────────┐
       │                 │                 │
       ▼                 ▼                 ▼
  門市營運模組       群組管理模組       定期收款模組
  Store Ops          Group Ops          Billing
```

三個模組共享同一套：

- User
- LINE Identity
- LINE Login
- LIFF
- Messaging API
- Webhook
- Rich Menu
- Merchant / Workspace
- Permission
- Notification
- Scheduler
- Payment
- Audit Log
- PostgreSQL / Prisma

---

## 4. 合併後 Repository 目標架構

```text
LINE/
│
├── apps/
│   ├── web/
│   │   ├── src/
│   │   ├── public/
│   │   └── package.json
│   │
│   └── desktop-agent/
│       ├── automation/
│       ├── gui/
│       ├── core/
│       ├── api/
│       ├── tests/
│       └── main.py
│
├── modules/
│   ├── store/
│   │   ├── queue/
│   │   ├── reservation/
│   │   ├── catalog/
│   │   ├── order/
│   │   └── staff/
│   │
│   ├── groups/
│   │   ├── tasks/
│   │   ├── whitelist/
│   │   ├── dissolution/
│   │   ├── events/
│   │   └── automation/
│   │
│   └── billing/
│       ├── plans/
│       ├── subscriptions/
│       ├── invoices/
│       ├── reminders/
│       └── collections/
│
├── core/
│   ├── auth/
│   ├── users/
│   ├── merchants/
│   ├── line/
│   ├── payments/
│   ├── notifications/
│   ├── scheduler/
│   ├── audit/
│   └── permissions/
│
├── prisma/
│   ├── schema.prisma
│   └── migrations/
│
├── docs/
│   ├── architecture/
│   ├── modules/
│   ├── adr/
│   └── migration/
│
├── tests/
├── .env.example
├── README.md
└── AGENTS.md
```

---

## 5. Shared Core

### 5.1 Core Auth

統一處理：

- LINE Login
- LIFF Login
- JWT / Session
- LINE Identity
- API Authentication
- Desktop Agent Authentication
- Staff Authentication

禁止各 Module 再建立第二套 LINE Login。

### 5.2 Core Users

現況：

```text
LINE群組管理：lineUserId
定期支付：lineUid
```

合併後統一：

```text
User
├── id
├── lineUserId
├── displayName
├── pictureUrl
├── createdAt
├── updatedAt
└── lastSeenAt
```

同一個 LINE 帳號在三個 Module 只對應一個 User。

### 5.3 Core Merchant / Workspace

同一商家可同時啟用：

- STORE
- GROUPS
- BILLING

因此 Merchant / Workspace 為平台最上層租戶。

### 5.4 Core LINE

集中處理：

```text
core/line/
├── login
├── liff
├── messaging
├── webhook
├── rich-menu
├── flex-message
├── signatures
└── client
```

### 5.5 Core Notifications

統一：

- LINE Push
- LINE Reply
- Flex Message
- 未來 Email
- 未來 SMS

Module 只決定「何時通知、通知內容」，不重複實作 LINE Client。

### 5.6 Core Scheduler / Jobs

統一管理：

```text
jobs/
├── reservation-reminder
├── recurring-billing
├── payment-reminder
├── group-dissolution-check
├── group-expiry
└── notification-retry
```

### 5.7 Core Payment

以現有 LINE群組管理 Payment Domain 為主體，保留並擴充：

- LINE Pay
- Cash
- Manual Payment
- 一次付款
- 訂單付款
- 項目級付款
- 部分付款
- 定期帳單付款
- Payment Confirm
- Idempotency
- Payment State Machine

### 5.8 Core Audit

統一 Audit Log：

```text
AuditLog
├── id
├── merchantId
├── actorUserId
├── actorType
├── module
├── action
├── resourceType
├── resourceId
├── metadata
└── createdAt
```

module：

- CORE
- STORE
- GROUPS
- BILLING

---

## 6. Module 1：Store Ops

來源：原 `LINE群組管理`

主要 Domain：

- Branch
- BranchSetting
- BusinessHour
- BusinessHourException
- ReservationSlot
- Reservation
- Queue
- QueueTicket
- QueuePosition
- QueueEvent
- CatalogItem
- Order
- OrderItem

完整保留：

- 預約
- 現場取號
- 即時叫號
- 插隊
- 過號
- 回叫
- 點餐
- 服務
- 商品
- 訂單
- 收款
- LINE Pay
- 員工
- 管理員
- 多分店
- Rich Menu

---

## 7. Module 2：Group Ops

來源：原 `LINE退群`

主要功能：

- 群組掃描
- 群組搜尋
- 群組清理
- 白名單
- 批次退出
- 即時模式
- 72 小時公示
- 解散任務
- 成員取消
- Bot 公告
- Group Audit
- Rate Limit
- Desktop Automation
- EXE Client

建議 Cloud Domain：

```text
GroupCleanupTask
GroupWhitelist
GroupAutomationEvent
DesktopAgent
DesktopAgentJob
```

---

## 8. Module 3：Billing

來源：原 `定期支付`

既有語意完整保留。

長期 Domain 建議：

```text
BillingPlan
Subscription
Invoice
```

對應既有：

```text
Project      → BillingPlan
Membership   → Subscription
Transaction  → Invoice
```

保留：

- 建立收款專案
- 固定金額
- 每月繳費日
- 成員加入
- INITIATOR
- PAYER
- 每月付款義務
- UNPAID
- PENDING
- COMPLETED
- OVERDUE
- LINE 提醒
- LIFF
- 付款確認
- 付款歷史

---

## 9. Windows Desktop Agent

LINE退群的 Desktop Automation 不應硬改為 Next.js。

正確架構：

```text
LINE Platform Cloud
        │
        │ HTTPS API
        ▼
Desktop Agent
        │
        │ Windows UI Automation
        ▼
LINE Desktop
```

Desktop Agent 保留：

- Python
- PyAutoGUI
- Windows UI Automation
- GUI
- EXE
- 本機 LINE 控制
- ui_mapping
- 本機緊急停止

Cloud 負責：

- User
- Task
- Whitelist
- Scheduler
- Audit
- Security
- LINE Bot
- Webhook
- Cancellation

Agent 流程：

```text
1. Agent 啟動
2. Agent Authentication
3. GET /api/v1/agent/jobs
4. 取得待執行任務
5. 執行 LINE Desktop Automation
6. POST /api/v1/agent/jobs/{id}/result
7. Cloud 更新狀態
8. Audit Log
```

---

## 10. 統一 Web App

建議：

```text
/
├── /app
│   ├── /store
│   ├── /groups
│   └── /billing
│
├── /customer
├── /staff
├── /admin
└── /api/v1
```

既有 `/c`、`/s` 路由可先保留相容性。

Dashboard：

```text
LINE 營運平台

[ 門市營運 ]
預約｜取號｜叫號｜訂單｜付款

[ 群組管理 ]
群組掃描｜白名單｜批次清理｜解散任務

[ 定期收款 ]
收款計畫｜成員｜帳單｜提醒｜付款
```

---

## 11. Module 啟用機制

建議：

```text
MerchantModule
├── merchantId
├── module
├── enabled
└── settings
```

module：

```text
STORE
GROUPS
BILLING
```

每個 Merchant 可獨立啟用或停用。

---

## 12. 資料庫整合

現況：

```text
LINE群組管理 → PostgreSQL / Prisma
定期支付     → PostgreSQL / Prisma
LINE退群     → JSON / Google Sheets
```

合併後：

```text
Cloud Source of Truth
→ PostgreSQL / Prisma
```

Google Sheets 功能不刪除，而是改為：

- Import
- Export
- Backup
- Reporting Adapter

---

## 13. API 架構

統一：

```text
/api/v1
```

Core：

```text
/api/v1/auth
/api/v1/me
/api/v1/merchants
/api/v1/branches
/api/v1/line
/api/v1/webhooks
/api/v1/notifications
```

Store：

```text
/api/v1/store/queue
/api/v1/store/reservations
/api/v1/store/orders
/api/v1/store/catalog
/api/v1/store/staff
```

Groups：

```text
/api/v1/groups
/api/v1/groups/tasks
/api/v1/groups/whitelist
/api/v1/groups/events
/api/v1/agent/jobs
```

Billing：

```text
/api/v1/billing/plans
/api/v1/billing/subscriptions
/api/v1/billing/invoices
/api/v1/billing/payments
```

既有 API 先保留相容，再逐步轉換 namespace。

---

## 14. LINE Webhook

統一入口：

```text
POST /api/v1/webhooks/line
```

架構：

```text
LINE Event
    │
    ▼
Webhook Router
    │
    ├── Core Handler
    ├── Store Handler
    ├── Groups Handler
    └── Billing Handler
```

不再讓三個 Module 維護三套 LINE webhook。

---

## 15. Rich Menu

Rich Menu Resolver 根據：

- User
- Role
- Merchant
- Enabled Modules
- Branch Scope

決定使用者看到哪套 Rich Menu。

所有 Rich Menu 操作由 Core LINE 管理。

---

## 16. 權限

延續現有：

- OWNER
- MANAGER
- STAFF
- CUSTOMER

並逐步增加 Capability：

```text
store.queue.manage
store.orders.manage
groups.cleanup.execute
groups.whitelist.manage
billing.plan.manage
billing.invoice.confirm
```

---

## 17. 不捨棄既有功能原則

### 原則 1

不因架構整合而刪除任何現有功能。

### 原則 2

功能可以搬移，但不可無故消失。

### 原則 3

先建立 Compatibility Layer，再刪除重複程式。

### 原則 4

任何既有流程在新版完成替代前不得移除。

### 原則 5

合併不是 Rewrite。

優先：

```text
Move
Reuse
Adapter
Compatibility
Refactor
```

最後才考慮 Rewrite。

---

## 18. 遷移計畫

### Phase 0：建立安全基線

- 確認三個專案目前可執行
- 建立功能清單
- 建立 smoke tests
- 記錄 API
- 記錄資料模型
- 記錄環境變數
- 保存 migration 前基線

### Phase 1：以 LINE群組管理作為 Web Core

不建立第四套主系統。

將現有 LINE群組管理逐步成為：

```text
apps/web
```

因為它目前已具備最完整的：

- PostgreSQL
- Prisma
- User
- Merchant
- Branch
- RBAC
- LINE
- Payment
- Audit
- API
- UI

### Phase 2：抽 Shared Core

整理：

```text
auth
users
merchant
line
notifications
audit
permissions
payment
scheduler
```

此階段只抽象共用能力，不改使用者行為。

### Phase 3：合併定期支付

依序：

1. User 合併
2. LINE Login 合併
3. LINE Client 合併
4. Project / Membership / Transaction 搬入 Billing Module
5. Cron 搬入 Shared Scheduler
6. Push 搬入 Notification Core
7. 付款流程接 Core Payment
8. 保留既有 UI / API 相容性

### Phase 4：合併 LINE退群 Cloud

搬入 Groups Module：

- Tasks
- Whitelist
- Audit
- Scheduler
- LINE Bot
- Webhook
- Security
- Cancellation

Google Sheets / JSON 逐步轉為 PostgreSQL。

但保留 Import / Export。

### Phase 5：Desktop Agent API 化

Python 保留：

- Automation
- GUI
- Windows UI Controller
- EXE
- 本機設定
- Rate Limit

新增：

```text
Agent API Client
```

讓 Agent 與 Cloud 同步任務。

### Phase 6：統一 Portal

建立：

```text
Dashboard
├── Store
├── Groups
└── Billing
```

### Phase 7：移除重複實作

只有新版完全取代後，才能移除：

- 重複 LINE Client
- 重複 Auth
- 重複 Webhook
- 重複 Payment
- 重複 Scheduler
- 重複 Audit
- 重複 User Model

---

## 19. 最終高階資料關係

```text
User
 │
 ├── MerchantMember
 │       │
 │       └── Merchant
 │             │
 │             ├── Branch
 │             │    ├── Queue
 │             │    ├── Reservation
 │             │    └── Order
 │             │
 │             ├── BillingPlan
 │             │    └── Subscription
 │             │           └── Invoice
 │             │                  └── Payment
 │             │
 │             └── GroupCleanupTask
 │                    └── GroupAutomationEvent
 │
 └── AuditLog
```

---

## 20. 合併成功條件

只有以下條件全部成立，才算整併完成：

1. 三個原專案功能都仍可使用。
2. 同一 LINE User 不再存在多套 Identity。
3. LINE Login 只維護一套。
4. LINE Webhook 只維護一個主要入口。
5. Messaging / Push / LIFF 使用共用 Core。
6. Payment 不再維護互不相容的重複實作。
7. Scheduler 統一管理。
8. Audit Log 可跨三個 Module 查詢。
9. Windows Agent 可從 Cloud 取得任務並回報。
10. 使用者可從同一 Portal 進入三大 Module。
11. 三個 Module 可獨立啟用或停用。
12. 整併過程沒有因重構而遺失既有功能。

---

## 21. 開發原則

後續 Agent / Codex / Antigravity 執行整併時，應遵守：

> Preserve behavior first, refactor second.

流程：

```text
先確保功能不變
↓
再抽共用模組
↓
再整理架構
↓
最後才刪除重複程式
```

禁止：

```text
為了目錄漂亮
→ 直接 Rewrite
→ 造成既有功能消失
```

整個專案最終方向：

> **一個 LINE Platform Core + 三個可獨立啟用的產品 Module + 一個 Windows Desktop Agent。**
